# Config Section Manifest
#
# Generated by scripts/gen-config-manifest.mts — do not edit by hand.
# Regenerate with: pnpm --filter @kiki/agent-core-v2 gen:config-manifest
#
# One [table] per registered config section, in the on-disk config.toml shape
# (snake_case keys). Un-commented assignments are registered defaults;
# commented "# field: type" lines describe the remaining schema fields.
# Values resolve as: default -> config.toml -> env overlay -> memory.

# Index (37 sections · 1 overlay(s))
#   agentExecutors                  src/app/agentExecutor/configSection.ts
#   agents                          src/session/agentCollaboration/configSection.ts
#   background                      src/agent/task/configSection.ts
#   builtinProductSkills            src/app/skillCatalog/configSection.ts
#   cron                            src/app/cron/configSection.ts
#   defaultPermissionMode           src/agent/permissionMode/configSection.ts
#   defaultPlanMode                 src/features/plan/configSection.ts
#   disabledNamedProfiles           src/workspace/workspaceAgentProfileLoader/configSection.ts
#   experimental                    src/app/flag/flag.ts
#   extraAgentDirs                  src/workspace/workspaceAgentProfileLoader/configSection.ts
#   extraSkillDirs                  src/app/skillCatalog/configSection.ts
#   hooks                           src/features/externalHooks/configSection.ts
#   identity                        src/app/agentIdentity/configSection.ts
#   image                           src/agent/media/configSection.ts
#   loopControl                     src/agent/loop/configSection.ts
#   mcp                             src/app/mcpConfig/configSection.ts
#   mergeAllAvailableSkills         src/app/skillCatalog/configSection.ts
#   models                          src/app/kosongConfig/configSection.ts
#   nbSearch                        src/app/nbSearch/configSection.ts
#   nbSearchSource                  src/app/nbSearch/configSection.ts
#   permission                      src/agent/permissionRules/configSection.ts
#   plan                            src/features/plan/configSection.ts
#   plugins                         src/app/plugin/configSection.ts
#   prompt                          src/app/prompt/configSection.ts
#   providers                       src/app/kosongConfig/configSection.ts
#   requestIdentity                 src/app/kosongConfig/configSection.ts
#   retry                           src/agent/stepRetry/configSection.ts
#   sessionResidency                src/app/sessionManager/configSection.ts
#   sessionTitle                    src/session/sessionTitle/configSection.ts
#   skipBuiltinProfileInstallation  src/workspace/workspaceAgentProfileLoader/configSection.ts
#   subagent                        src/session/subagent/configSection.ts
#   task                            src/agent/task/configSection.ts
#   thinking                        src/app/kosongConfig/configSection.ts
#   threadCommunication             src/app/threadCommunication/configSection.ts
#   tokenCounting                   src/agent/tokenCounting/configSection.ts
#   tools                           src/agent/toolPolicy/configSection.ts
#   workspaceInstance               src/workspace/workspaceInstance/configSection.ts
#   (overlay) kimiModelEnvOverlay  src/app/kosongConfig/envOverlay.ts

# ##########################################################################
# agentExecutors (config.toml: agent_executors)
#   owner: src/app/agentExecutor/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[agent_executors]

# one [agent_executors."<name>"] table per entry:
# [agent_executors."<name>"]
  # protocol: string
  # command: string
  # sources: object | object | object | object[]
  # source: string
  # version_probe: object
    # args: string[]
  # args: string[] (default: [])
  # env: record<string, string>
  # startup_timeout_ms: integer
  # shutdown_grace_ms: integer
  # model_binding: string
  # model_args: string[]
  # model_config_category: string
  # model_config_id: string
  # thought_config_category: string
  # thought_config_id: string
  # permission_mode_mapping: object
    # config_id: string
    # config_category: string
    # manual: string | boolean
    # auto: string | boolean
    # yolo: string | boolean
  # revision: string

# ##########################################################################
# agents
#   owner: src/session/agentCollaboration/configSection.ts
#   scope: core
# ##########################################################################

[agents]
enabled = true
notify_parent = true

# [agents.delegation]
  # sub: boolean
  # independent: boolean

# ##########################################################################
# background
#   owner: src/agent/task/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     keep_alive_on_exit <- KIKI_BACKGROUND_KEEP_ALIVE_ON_EXIT (custom parse)
#     max_running_tasks <- KIKI_BACKGROUND_MAX_RUNNING_TASKS (custom parse)
# ##########################################################################

[background]
# max_running_tasks: integer
# keep_alive_on_exit: boolean
# bash_auto_background_on_timeout: boolean
# bash_task_timeout_s: integer
# kill_grace_period_ms: integer
# print_wait_ceiling_s: integer
# print_background_mode: "exit" | "drain" | "steer"
# print_max_turns: integer

# ##########################################################################
# builtinProductSkills (config.toml: builtin_product_skills)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#      <- KIKI_BUILTIN_PRODUCT_SKILLS (custom parse)
# ##########################################################################

builtin_product_skills = true

# ##########################################################################
# cron
#   owner: src/app/cron/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     debug <- KIKI_CRON_DEBUG (custom parse)
#     no_jitter <- KIKI_CRON_NO_JITTER (custom parse)
#     no_stale <- KIKI_CRON_NO_STALE (custom parse)
#     disabled <- KIKI_DISABLE_CRON (custom parse)
#     manual_tick <- KIKI_CRON_MANUAL_TICK (custom parse)
#     clock <- KIKI_CRON_CLOCK
#     poll_interval_ms <- KIKI_CRON_POLL_INTERVAL_MS (custom parse)
# ##########################################################################

[cron]
# (schema uses transforms; fields below come from the registered default)
debug = false
no_jitter = false
no_stale = false
disabled = false
manual_tick = false

# ##########################################################################
# defaultPermissionMode (config.toml: default_permission_mode)
#   owner: src/agent/permissionMode/configSection.ts
#   scope: core
# ##########################################################################

# default_permission_mode: "manual" | "auto" | "yolo"

# ##########################################################################
# defaultPlanMode (config.toml: default_plan_mode)
#   owner: src/features/plan/configSection.ts
#   scope: core
# ##########################################################################

default_plan_mode = false

# ##########################################################################
# disabledNamedProfiles (config.toml: disabled_named_profiles)
#   owner: src/workspace/workspaceAgentProfileLoader/configSection.ts
#   scope: core
# ##########################################################################

disabled_named_profiles = []

# ##########################################################################
# experimental
#   owner: src/app/flag/flag.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[experimental]
# <name>: boolean

# ##########################################################################
# extraAgentDirs (config.toml: extra_agent_dirs)
#   owner: src/workspace/workspaceAgentProfileLoader/configSection.ts
#   scope: core
# ##########################################################################

extra_agent_dirs = []

# ##########################################################################
# extraSkillDirs (config.toml: extra_skill_dirs)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
# ##########################################################################

extra_skill_dirs = []

# ##########################################################################
# hooks
#   owner: src/features/externalHooks/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

# one [[hooks]] table per entry:
# [[hooks]]
  # event: "PreToolUse" | "PostToolUse" | "PostToolUseFailure" | "PermissionRequest" | "PermissionResult" | "UserPromptSubmit" | "UserPromptQueued" | "TurnStarted" | "Stop" | "StopFailure" | "Interrupt" | "SessionStart" | "SessionEnd" | "SessionHeartbeat" | "SubagentStart" | "SubagentStop" | "TaskStarted" | "PreCompact" | "PostCompact" | "Notification"
  # matcher: string
  # command: string
  # timeout: integer

# ##########################################################################
# identity
#   owner: src/app/agentIdentity/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     name <- KIKI_IDENTITY_NAME (custom parse)
#     slug <- KIKI_IDENTITY_SLUG (custom parse)
# ##########################################################################

[identity]
# name: string
# slug: string
advertise_as_kimi_code = false

# ##########################################################################
# image
#   owner: src/agent/media/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     max_edge_px <- KIKI_IMAGE_MAX_EDGE_PX (custom parse)
#     read_byte_budget <- KIKI_IMAGE_READ_BYTE_BUDGET (custom parse)
# ##########################################################################

[image]
# max_edge_px: integer
# read_byte_budget: integer

# ##########################################################################
# loopControl (config.toml: loop_control)
#   owner: src/agent/loop/configSection.ts
#   scope: core
#   hooks: custom toToml · stripEnv
#   env:
#     max_steps_per_turn <- KIKI_LOOP_MAX_STEPS_PER_TURN (custom parse)
#     max_attempts_per_step <- KIKI_LOOP_MAX_ATTEMPTS_PER_STEP (custom parse)
#     compaction_soft_context_size <- KIKI_LOOP_COMPACTION_SOFT_CONTEXT_SIZE (custom parse)
# ##########################################################################

[loop_control]
# max_steps_per_turn: integer
# max_attempts_per_step: integer
# max_ralph_iterations: integer
# reserved_context_size: integer
# compaction_trigger_ratio: number
# compaction_max_attempts: integer
compaction_soft_context_size = 0

# ##########################################################################
# mcp
#   owner: src/app/mcpConfig/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     startup_timeout_ms <- KIKI_MCP_STARTUP_TIMEOUT_MS (custom parse)
#     tool_timeout_ms <- KIKI_MCP_TOOL_TIMEOUT_MS (custom parse)
# ##########################################################################

[mcp]
# startup_timeout_ms: integer
# tool_timeout_ms: integer

# ##########################################################################
# mergeAllAvailableSkills (config.toml: merge_all_available_skills)
#   owner: src/app/skillCatalog/configSection.ts
#   scope: core
# ##########################################################################

merge_all_available_skills = true

# ##########################################################################
# models
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[models]

# one [models."<name>"] table per entry:
# [models."<name>"]
  # provider_id: string
  # base_url: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # protocol: "anthropic" | "openai" | "openai_responses" | "google-genai"
  # name: string
  # aliases: string[]
  # provider: string
  # model: string
  # max_context_size: integer
  # max_input_size: integer
  # max_output_size: integer
  # capabilities: string[]
  # display_name: string
  # reasoning_key: string
  # adaptive_thinking: boolean
  # beta_api: boolean
  # support_efforts: string[]
  # default_effort: string
  # off_effort: string
  # context_budget: integer
  # max_completion_tokens: integer
  # service_tier: "auto" | "default" | "flex" | "priority"
  # request_params: record<string, string | number | boolean>
  # images: object
    # accepted_types: "image/png" | "image/jpeg" | "image/gif" | "image/webp" | "image/bmp" | "image/heic" | "image/heif" | "image/avif" | "image/tiff" | "image/x-icon"[]
    # convert_unsupported: "off" | "auto" | "png" | "jpeg"
  # overrides: object
    # max_context_size: integer
    # max_input_size: integer
    # max_output_size: integer
    # capabilities: string[]
    # display_name: string
    # reasoning_key: string
    # adaptive_thinking: boolean
    # support_efforts: string[]
    # default_effort: string
    # off_effort: string
    # context_budget: integer
    # max_completion_tokens: integer
    # service_tier: "auto" | "default" | "flex" | "priority"
    # request_params: record<string, string | number | boolean>
  # cognition: object
    # overlay: string | string[]
    # steering: string | string[]
    # anchor: string | string[]
    # overlay_mode: "append" | "prepend" | "wrap" | "persona" | "replace"
    # anchor_steps: integer
    # anchor_scope: "session" | "turn"
  # prompt_overrides: object
    # files: string[]
    # fields: record<string, string>
  # request_identity: object
    # preset: "codex_compatible" | "grok_build_compatible" | "kimi_code" | "none"
    # overrides: object
      # lineage: object
        # format: "codex" | "grok_build" | "kimi_code" | "none"
        # session_scope: "shared_session" | "agent_session" | "none"
        # thread_identity: "agent" | "none"
        # parent_thread: "immediate_agent" | "none"
        # subagent_marker: "enabled" | "none"
        # turn_ancestry: "spawn_context" | "none"
      # client: object
        # installation_identity: "persistent_local" | "none"
        # originator: object | object | object
        # user_agent: "codex" | "grok_build" | "kimi_code" | "host" | "none"
      # request: object
        # logical_id: "turn" | "none"
        # turn_index: "agent_session" | "none"
      # cache: object
        # source: "session" | "none"
        # responses: "prompt_cache_key" | "none"
        # messages: "metadata_user_id" | "none"
      # responses_metadata: "codex" | "none"

# ##########################################################################
# nbSearch (config.toml: nb_search)
#   owner: src/app/nbSearch/configSection.ts
#   scope: core
#   hooks: custom merge · custom fromToml · custom toToml
# ##########################################################################

[nb_search]
# (schema uses transforms; see the owner file)

# ##########################################################################
# nbSearchSource (config.toml: nb_search_source)
#   owner: src/app/nbSearch/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[nb_search_source]
reuse_local_config = true

# ##########################################################################
# permission
#   owner: src/agent/permissionRules/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[permission]
# rules: object[] — one entry per item:
  # decision: "allow" | "deny" | "ask"
  # scope: "turn-override" | "session-runtime" | "project" | "user" (default: "user")
  # pattern: string
  # reason: string
# dangerous_bash: "on" | "off" | "default"

# ##########################################################################
# plan
#   owner: src/features/plan/configSection.ts
#   scope: core
# ##########################################################################

[plan]
gate = "free"
enter_approval_timeout_ms = 60000

# ##########################################################################
# plugins
#   owner: src/app/plugin/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     marketplace_url <- KIKI_PLUGIN_MARKETPLACE_URL
# ##########################################################################

[plugins]
# marketplace_url: string

# ##########################################################################
# prompt
#   owner: src/app/prompt/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[prompt]
# variables: record<string, string>

# [prompt.overrides]
  # files: string[]
  # fields: record<string, string>

# ##########################################################################
# providers
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml · stripEnv
#   env:
#     __kimi_env__.api_key <- KIKI_MODEL_API_KEY
#     __kimi_env__.type <- KIKI_MODEL_PROVIDER_TYPE
#     __kimi_env__.base_url <- KIKI_MODEL_BASE_URL
# ##########################################################################

[providers]

# one [providers."<name>"] table per entry:
# [providers."<name>"]
  # model_source: "static" | "discover" | "oauth-catalog"
  # base_url: string
  # custom_headers: record<string, string>
  # default_model: string
  # request_identity: object
    # preset: "codex_compatible" | "grok_build_compatible" | "kimi_code" | "none"
    # overrides: object
      # lineage: object
        # format: "codex" | "grok_build" | "kimi_code" | "none"
        # session_scope: "shared_session" | "agent_session" | "none"
        # thread_identity: "agent" | "none"
        # parent_thread: "immediate_agent" | "none"
        # subagent_marker: "enabled" | "none"
        # turn_ancestry: "spawn_context" | "none"
      # client: object
        # installation_identity: "persistent_local" | "none"
        # originator: object | object | object
        # user_agent: "codex" | "grok_build" | "kimi_code" | "host" | "none"
      # request: object
        # logical_id: "turn" | "none"
        # turn_index: "agent_session" | "none"
      # cache: object
        # source: "session" | "none"
        # responses: "prompt_cache_key" | "none"
        # messages: "metadata_user_id" | "none"
      # responses_metadata: "codex" | "none"
  # images: object
    # accepted_types: "image/png" | "image/jpeg" | "image/gif" | "image/webp" | "image/bmp" | "image/heic" | "image/heif" | "image/avif" | "image/tiff" | "image/x-icon"[]
    # convert_unsupported: "off" | "auto" | "png" | "jpeg"
  # type: string
  # api_key: string
  # oauth: object
    # storage: "file" | "keyring"
    # key: string
    # oauth_host: string
  # env: record<string, string>
  # source: record<string, any>

# ##########################################################################
# requestIdentity (config.toml: request_identity)
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[request_identity]
# preset: "codex_compatible" | "grok_build_compatible" | "kimi_code" | "none"

# [request_identity.overrides]
  # lineage: object
    # format: "codex" | "grok_build" | "kimi_code" | "none"
    # session_scope: "shared_session" | "agent_session" | "none"
    # thread_identity: "agent" | "none"
    # parent_thread: "immediate_agent" | "none"
    # subagent_marker: "enabled" | "none"
    # turn_ancestry: "spawn_context" | "none"
  # client: object
    # installation_identity: "persistent_local" | "none"
    # originator: object | object | object
    # user_agent: "codex" | "grok_build" | "kimi_code" | "host" | "none"
  # request: object
    # logical_id: "turn" | "none"
    # turn_index: "agent_session" | "none"
  # cache: object
    # source: "session" | "none"
    # responses: "prompt_cache_key" | "none"
    # messages: "metadata_user_id" | "none"
  # responses_metadata: "codex" | "none"

# ##########################################################################
# retry
#   owner: src/agent/stepRetry/configSection.ts
#   scope: core
#   hooks: custom fromToml · custom toToml
# ##########################################################################

[retry]
# max_attempts: integer
# policies: object[] — one entry per item:
  # match: string
  # max_attempts: integer
  # backoff: integer
  # retry: boolean (default: true)

# ##########################################################################
# sessionResidency (config.toml: session_residency)
#   owner: src/app/sessionManager/configSection.ts
#   scope: core
# ##########################################################################

[session_residency]
idle_ttl_ms = 600000
max_live_sessions = 8
min_idle_ms = 60000
sweep_interval_ms = 30000
max_concurrent_restores = 1
max_queued_restores = 8

# ##########################################################################
# sessionTitle (config.toml: session_title)
#   owner: src/session/sessionTitle/configSection.ts
#   scope: core
# ##########################################################################

[session_title]
# model: string

# ##########################################################################
# skipBuiltinProfileInstallation (config.toml: skip_builtin_profile_installation)
#   owner: src/workspace/workspaceAgentProfileLoader/configSection.ts
#   scope: core
# ##########################################################################

# skip_builtin_profile_installation: string[]

# ##########################################################################
# subagent
#   owner: src/session/subagent/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     timeout_ms <- KIKI_SUBAGENT_TIMEOUT_MS (custom parse)
# ##########################################################################

[subagent]
timeout_ms = 7200000
# deny_models: string[]
max_direct_children = 16
max_total_subagents = 0
default_profile = "general"
allowed_tools = []

# ##########################################################################
# task
#   owner: src/agent/task/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     keep_alive_on_exit <- KIKI_BACKGROUND_KEEP_ALIVE_ON_EXIT (custom parse)
#     max_running_tasks <- KIKI_BACKGROUND_MAX_RUNNING_TASKS (custom parse)
# ##########################################################################

[task]
# max_running_tasks: integer
# keep_alive_on_exit: boolean
# bash_auto_background_on_timeout: boolean
# bash_task_timeout_s: integer
# kill_grace_period_ms: integer
# print_wait_ceiling_s: integer
# print_background_mode: "exit" | "drain" | "steer"
# print_max_turns: integer

# ##########################################################################
# thinking
#   owner: src/app/kosongConfig/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     forced_effort <- KIKI_MODEL_THINKING_EFFORT
# ##########################################################################

[thinking]
# enabled: boolean
# effort: string
# forced_effort: string
# keep: string

# ##########################################################################
# threadCommunication (config.toml: thread_communication)
#   owner: src/app/threadCommunication/configSection.ts
#   scope: core
# ##########################################################################

[thread_communication]
enabled = false

# ##########################################################################
# tokenCounting (config.toml: token_counting)
#   owner: src/agent/tokenCounting/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     strategy <- KIKI_TOKEN_COUNTING_STRATEGY (custom parse)
# ##########################################################################

[token_counting]
strategy = "measured+estimated"

# ##########################################################################
# tools
#   owner: src/agent/toolPolicy/configSection.ts
#   scope: core
# ##########################################################################

[tools]
# enabled: string[]
# disabled: string[]

# ##########################################################################
# workspaceInstance (config.toml: workspace_instance)
#   owner: src/workspace/workspaceInstance/configSection.ts
#   scope: core
#   hooks: stripEnv
#   env:
#     idle_ttl_ms <- KIKI_WORKSPACE_IDLE_TTL_MS (custom parse)
# ##########################################################################

[workspace_instance]
idle_ttl_ms = 300000
