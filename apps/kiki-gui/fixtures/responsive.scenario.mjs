/**
 * responsive — viewport-width sweep (1440/1024/768/320) over a populated
 * session and the settings panel. Data is identical to the settings
 * scenario; the proof runner only varies the viewport.
 */

export { default } from './settings.scenario.mjs';
