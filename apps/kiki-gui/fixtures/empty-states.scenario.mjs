/**
 * empty-states — no sessions at all: the sidebar's "No sessions yet" line and
 * the main area's watermark empty state. (A session created through the UI
 * lands in the blank-page state, which the proof also captures.)
 */

export default {
  sessions: [],
  snapshots: {},
};
