/**
 * settings-agents — the settings demo scenario reused for the named-agent
 * management proofs (merged view, dual disable channels). Kept as an alias
 * so the proof runner can switch scenarios by name without disturbing the
 * other settings walkers.
 */

export { default } from './settings.scenario.mjs';
