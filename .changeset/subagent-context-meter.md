---
"@kiki/cli": patch
---

Show the shared context-usage meter in subagent composers, with the agent's own context window and lifetime token totals; unpriced per-agent projections hide the cost row instead of reading as $0.00.
