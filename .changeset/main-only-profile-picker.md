---
"@kiki/cli": patch
---

Restore the session profile pickers to offering only enabled main profiles; sub-agent-only profiles are dispatched with AgentRun instead of being selectable as the session's main agent.
