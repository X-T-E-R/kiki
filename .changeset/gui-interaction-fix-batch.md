---
"@kiki/gui": patch
---

Fix in-app page navigation (internal links no longer force a new window), bound the sibling/children agent lists with an expander, make the context meter open a details panel that hosts the compact action, cap the right-rail subagent tree in a scroll region, and make effort selection consistent between display and submission (select-to-send) in both new-session and existing-session composers.
