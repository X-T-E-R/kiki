---
"@kiki/cli": patch
---

Opening or switching to a large cold session no longer stalls on a hidden full-session resume from the capabilities and skills panels, and agent-panel token metrics are cached per agent instead of re-scanning every agent's log on each refresh.
