---
"@kiki/cli": patch
---

Collapsible tool output: long command output, tool input/output blocks, and task output now clamp to a max height with an inline show more/less toggle instead of always scrolling. Subagent cards show a hover tooltip with the description or error, and the agent identity summary is expandable.
