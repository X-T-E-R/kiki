---
"@kiki/cli": minor
---

Add a built-in `kiki-profile` skill that guides creating and editing agent profile files and `SYSTEM.md`, covering load paths, the closed frontmatter key set, and the replace-by-default prompt semantics.
