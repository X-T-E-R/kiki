---
"@kiki/gui": patch
---

Rework the subagent detail page and right rail: the detail page now shows the spawn instruction from the parent agent in a distinct card, related-agent chips truncate long names and the whole relations strip folds when crowded, right-rail chapters are collapsible with bounded todo/task lists, and usage reads live session counters with cost surfaces hidden until a server reports spend.
