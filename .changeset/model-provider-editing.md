---
"@kiki/cli": minor
---

Add entity-level editing for model providers and models in settings: each provider or model saves independently with conflict detection, so concurrent edits no longer silently overwrite each other.
