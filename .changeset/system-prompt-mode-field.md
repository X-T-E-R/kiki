---
"@kiki/agent-core-v2": minor
---

Agent profiles gain a `system_prompt_mode` field (`replace` / `prepend` / `append`), letting a profile inherit the default system prompt and only supply its own additions instead of forking the full template.
