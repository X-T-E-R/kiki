---
"@kiki/agent-core-v2": patch
---

Resolve nested subagent spawns from the configured pool default or the main agent's current model instead of inheriting the spawning subagent's own model, while keeping explicit and pool bindings fixed.
