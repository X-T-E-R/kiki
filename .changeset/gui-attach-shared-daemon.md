---
"@kiki/gui": patch
---

Attach the desktop app to an already-running Kiki server (found through the instance registry, authenticated with the home token) instead of always spawning a private sidecar, so sessions started by other clients appear in the GUI.
