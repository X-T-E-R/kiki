---
"@kiki/cli": patch
---

Reload custom agent files automatically when they change in user, project, or `extra_agent_dirs` directories; running sessions can dispatch new or updated agents without `/reload`.
