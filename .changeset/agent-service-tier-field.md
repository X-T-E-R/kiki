---
"@moonshot-ai/kimi-code": patch
---

Add the `service_tier` agent frontmatter field, sent on every request when the model uses an OpenAI Responses provider. Set `service_tier: priority` in an agent file to use it.
