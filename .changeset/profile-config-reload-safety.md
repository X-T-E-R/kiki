---
"@kiki/cli": patch
---

Fix configuration reloads dropping valid providers or models, profile resume failures when the default agent is unavailable, and missing AGENTS.md reminders for custom profiles.
