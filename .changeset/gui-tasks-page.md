---
"@kiki/gui": minor
---

Add a per-session Tasks page that lists all background tasks with status filters, expandable rows (command, output tail, timestamps), and stop for running tasks. Open it from the right rail's Background tasks chapter via "View all".
