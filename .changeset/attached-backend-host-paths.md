---
"@kiki/gui": patch
---

Fix "Reveal in folder" and other host-path actions failing with "Kiki backend is not connected" when the desktop shell is attached to an already-running backend instead of having spawned it.
