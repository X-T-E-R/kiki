---
"@kiki/cli": minor
---

Add a `[retry]` config section with `max_attempts` and per-error `policies` that match an error code or name to override the attempt budget and backoff, or to opt an error out of retrying.
