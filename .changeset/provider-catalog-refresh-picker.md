---
'@kiki/cli': minor
---

Fix provider model refresh reporting silent failures as "catalog unchanged", and add a searchable catalog-backed model picker that auto-fills model parameters in provider settings.
