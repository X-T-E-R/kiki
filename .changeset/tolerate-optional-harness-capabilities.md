---
"@kiki/cli": patch
"@kiki/acp-client": patch
---

Tolerate external harnesses that do not expose optional session capabilities (permission modes, thinking levels, additional directories, tools) instead of failing the dispatch.
