---
"@kiki/cli": minor
---

Remove the legacy Kimi migration surface (settings compatibility card, session badge, Tauri migration commands, and migration docs); legacy kimi-code paths are no longer read.
