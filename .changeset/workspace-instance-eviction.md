---
"@kiki/agent-core-v2": minor
"@kiki/kap-server": patch
---

Track live-session references to workspace instances, evict idle instances after a configurable TTL, dispose every instance during shutdown, and force-close live sessions when a workspace is deleted through the server API.
