---
"@kiki/cli": minor
---

Add the `kiki prompt-fields` command to list registered prompt fields, show a field's default and allowed variables, validate prompt override configuration, and explain a field's effective value with its full source chain.
