---
"@kiki/cli": minor
---

Redesign the composer agent-profile and model pickers with wider, searchable, group-headed rows, list every enabled non-private agent profile, and resolve an ambiguous model alias to the first matching catalog entry instead of an error.
