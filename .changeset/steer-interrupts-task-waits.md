---
"@kiki/cli": minor
---

Interrupt a running TaskWait immediately when you steer with new input instead of blocking until the wait times out; the wait returns an interrupted status and the background tasks keep running.
