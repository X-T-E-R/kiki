---
"@kiki/gui": patch
---

The session task board now opens scoped to the current workspace instead of aggregating every workspace, so it loads almost instantly; a scope switcher in the board header reaches the all-workspaces overview when needed.
