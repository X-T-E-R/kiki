---
"@moonshot-ai/kap-server": patch
---

Add loopback streamable HTTP for the Kiki MCP edge (`POST/GET/DELETE /mcp`), bind seats from the delegation bearer at connect time, and split `kiki_profiles` from `kiki_list`. stdio remains as a compatibility transport.
