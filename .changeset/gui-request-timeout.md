---
"@kiki/cli": minor
---

Add a request timeout setting (Settings → Connection, 5–600 s, default 30 s); the initial load of a large conversation no longer fails with a timeout and instead waits with its loading state.
