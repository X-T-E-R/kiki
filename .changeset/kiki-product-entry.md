---
"@kiki/cli": minor
---

Use `kiki` for CLI and native executables, `KIKI_*` product settings and `.kiki` home/project paths, with explicit non-overwriting migration of legacy configuration, credentials and authored assets.
